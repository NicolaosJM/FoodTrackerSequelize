<h1><i class="fas fa-carrot"></i>Food Diary<i class="fas fa-drumstick-bite"></i></h1>
<br>
<h2>Create a food diary to track your daily calorie intake.</h2>
<br>
<div class="container">
    <div class="row">
        <div class="col col-lg-5 col-sm-12">
            <h3 class="text-centered">Meals planned for the day:</h3>
            <div class="row">
                <ul>
                    {{#each foodtable}}
                        {{#unless this.devoured}}
                            <div class="col-s-10 mealList">
                                <p class="mealNames">{{this.meal_name}} - Calories: {{this.calorie_count}}</p>
                                
                            </div>
                            <form>            
                                    <input type="hidden" value="true" name="devoured" type="text">                                     {{!--  --}}
                                    <button type="submit" class="btn btn-warning change-devoured-state" data-id="{{id}}" data-devoured="{{devoured}}">Eaten</button>
                                </form>
                            <div class="col-s-2">
                                
                            </div>
                        {{/unless}}
                    {{/each}}
                </ul>
            </div>
        </div>

        <div class="col col-lg-2 col-sm-12"></div>
        <div class="col col-lg-5 col-sm-12">
            <h3 class="text-centered">Meals eaten today:</h3>
            <ul>
                {{#each foodtable}}
                    {{#if this.devoured}}
                        <input class="form-control" type="text" placeholder="{{this.meal_name}} - Calories: {{this.calorie_count}}" readonly>
                    {{/if}}
                {{/each}}
            </ul>

        </div>
    </div>
    <div class="row">
        <div class="col-s-12">
            <form class="create-form enterForm">
                <div class="form-group">
                    <label for="meal">Add a meal: </label>
                    <input type="text" id="meal" name="meal_name">
                    <label for="calories">Calorie content</label>
                    <input type="text" id="calories" name="calorie_count">
                </div>

                <button type="submit">Add meal</button>
            </form>
        </div>
    </div>
</div>